language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-yotest' ]; then cd .. && eval "mv $currentfolder generator-yotest" && cd generator-yotest; fi

